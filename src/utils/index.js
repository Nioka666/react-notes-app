export const noteAdd = (noteData) => {
    console.log(noteData);
}

export const noteEdit = (noteID) => {
    console.log(noteID);
}

export const noteArchive = (noteID) => {
    console.log(noteID);
}

export const noteDelete = (noteID) => {
    console.log(noteID);
}